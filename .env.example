# Emotion Pet - 环境变量示例
# 复制为 .env 并填入真实值。切勿提交 .env 文件。

# HuggingFace cache 目录（可选，避免C盘爆满）
# HF_HOME=D:/models/huggingface

# 若使用在线 LLM API 进行对话生成（可选，默认走本地规则模板）
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1

# 训练设备（cuda / cpu / mps）
DEVICE=cuda

# 实验随机种子
SEED=42
